# q286 mod-13-4 support schedule audit

Status: finite predeclared no-support schedule.  This is not a stress theorem,
signed correlation theorem, pointwise character-sum theorem, or Goldbach proof.

## Question

The immediate prospective block after the `target_mod_13 == 4` pocket was
frozen had no `full_nonpositive` rows.  This audit asks whether that was just
one empty block or whether the next broader predeclared schedule would produce
stress support for the frozen descriptor.

## Fixture

The descriptor stayed frozen:

```text
target_mod_13 == 4
source support:      6
source low count:    6
source above count:  0
source precision:    1.0
source recall:       0.18181818181818182
```

The schedule was declared before centered `(3,1)` scoring:

```text
q286 arithmetic period:       10010
block size:                   8 cycles
targets per cycle:            5005
future block indices:         2, 3, 4, 5
future starts:                170160, 250240, 330320, 410400
scheduled target count:       160160
```

## Result

No scheduled block produced a `full_nonpositive` row:

```text
start 170160: active_selector 529, rescued_first_three_tail 529, full_nonpositive 0
start 250240: active_selector 139, rescued_first_three_tail 139, full_nonpositive 0
start 330320: active_selector  63, rescued_first_three_tail  63, full_nonpositive 0
start 410400: active_selector  49, rescued_first_three_tail  49, full_nonpositive 0
```

Aggregate:

```text
scheduled targets:                    160160
scheduled full_nonpositive rows:           0
scheduled mod13-4 support:                0
scheduled status:                         untested_no_scheduled_mod13_4_full_nonpositive_support
```

Because support is zero, this schedule neither confirms nor falsifies the
`target_mod_13 == 4` low-`(3,1)` pocket.

## Interpretation

For the q286 `full_nonpositive` predicate, the post-discovery hole closes
locally across the immediate block and the next four scheduled blocks.  That is
finite evidence only; it is not an infinite closure theorem.  It does mean the
single-residue classifier route is currently support-starved rather than
failing by an observed above-threshold `mod13==4` row.

The proof lane should now shift away from hunting a single residue classifier
unless a larger support-search schedule is explicitly frozen first.  The more
promising route is to formulate the observed rescue as a distributed
cone/correlation estimate: the active/tail rows persist, but the complement
keeps rescuing them on these later blocks.

Generated by:

- `tools/build_q286_mod13_4_support_schedule_audit.py`
- `evidence/q286-mod13-4-support-schedule-audit.json`
