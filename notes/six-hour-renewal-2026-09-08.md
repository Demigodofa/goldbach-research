# Six-hour research renewal

Historical time authorization: Kevin subsequently withdrew the six-hour
cutoff and invited the chosen goal in `../RESEARCH_GOAL.md`. The deadline
below is retained as history and no longer controls the active research.

Owner: Kevin; execution and integration: Rill. Purpose: preserve the renewed
authorization, actual execution evidence, and next mathematical decision.

Kevin explicitly authorized six more hours at the live clock
2026-09-08 16:50:33 UTC (12:50:33 Eastern). The new deadline is
2026-09-08 22:50:33 UTC (18:50:33 Eastern). This replaces the expired time
window, while keeping the Goldbach investigation and its evidence rules.
No spending, publishing, contacting others, or foreground input is authorized.

Observable completion: substantive research through the authorized window,
preserved checked results and failed hypotheses, an honest unresolved-gap
report, and a final report at the deadline. A Goldbach proof is aspirational.
An active goal flag is not evidence of continued execution. The earlier gap
is retained in `execution-gap-2026-09-08.md` and must not be concealed.

Starting checkpoint: local commit d7d15d8, clean tree, 79 tests passing both
normally and with Python optimization after the earlier resumption. The
count pipeline and two-moment block certificate are finite verified tools;
neither proves universal positivity. No earlier shell computation was live.

First bounded decision: determine whether analytically justified first- and
second-moment bounds can replace computing every representation count, and
how precise those bounds must be. Two read-only lanes were actually started:
`block_moment_sources` (Terra/high, precise primary theorems) and
`moment_bound_review` (Sol/xhigh, proof/code and tolerance review). Root owns
edits and independent mathematical investigation. The fresh routing receipt
permits two spawned lanes. Local Qwen remains unavailable under the recorded
missing-runtime exception; no contribution is attributed to it.

Continuation support is under a bounded runtime check. Do not describe that
check or queued work as a guarantee of uninterrupted future execution.

At 16:58:47 UTC the reviewed exact-thread intent
`69a856cf-715b-4f1f-be20-86150b36f9a2` passed bridge dry run. Its single live
attempt then failed before process creation with Python `FileNotFoundError:
[WinError 2]` in the bridge's `subprocess.run(["codex", "queue", ...])`.
The Windows PATH lookup exposes npm `codex` and `codex.cmd`, rather than a
direct executable named `codex`. The supported shell help command works,
but this Python dispatch route does not. No continuation was queued by this
attempt; do not retry the duplicate-protected intent or claim queue success.
The bounded support check ends here; root continues the active mathematical
turn. The two review/source agents are live. No scheduled watcher was added.

First new deterministic probe: for a flat positive reference a and m=1000,
separate relative bounds S>=ma(1-e), T<=ma^2(1+e) imply positivity if
`m(1-e)^2-(m-1)(1+e)>0`. Exact rational evaluation passes e=1/3000, while
e=1/1000 fails. The explicit nonnegative vector (0,1000,...,1000), with
999 nonzero entries, satisfies the latter error premises and contains one
zero. This is a countermodel to an insufficient aggregate precision claim,
not an actual prime-count vector or a Goldbach counterexample.
