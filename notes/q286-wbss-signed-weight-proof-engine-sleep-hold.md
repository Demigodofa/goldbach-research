# q286-WBSS signed-weight proof-engine sleep/HOLD

Clarification, 2026-09-16: the independent candidate `M(a)*S(N)*N/3` is not
excluded by the `T_N`-dependent decomposition. See
[independent local density and the mass direction](q286-independent-local-density-and-mass-direction.md).
This corrects that interpretation, without supplying the pointwise remainder
bound needed to reactivate the proof engine. The centered-only route remains
unable to control the missing total-mass direction.

## Question

After the support-floor, aggregate-`L2`, principal-factor, and source-fit
audits, should the unchanged q286 signed-weight route remain active as a
standalone proof engine?

## Receipt

```text
tools/build_q286_wbss_signed_weight_proof_engine_sleep_hold.py
evidence/q286-wbss-signed-weight-proof-engine-sleep-hold.json
```

## Decision

`SLEEP_q286_signed_weight_proof_engine_until_raw_estimate`.

The exact current conjunction is dormant:

```text
support-only signed weight positivity:        blocked
normalized aggregate L2 as bridge:            blocked
principal local factor as independent support: blocked
named external signed-region bridge:          blocked
```

This does **not** kill every q286 component.  Preserved pieces:

```text
positive local factors:              M(a) min 0.6039353780830684
signed-shape margin:                 min margin 0.10534041937277128
K_286 quarter residual calibration:  max A_other/(M-H_286) 0.24495753331096648
visual/coefficient structure:        reservoir only
```

## Reactivation Triggers

Reactivate the lane only under a changed condition:

```text
1. A raw pointwise weighted binary-prime theorem for the exact signed q286 weight.
2. A materially new signed weight outside the recorded no-good constraints.
3. A raw active-character moment theorem that pays the target without assuming T_N>0.
4. A separate positive-mass theorem, explicitly labeled as Goldbach-strength input.
```

## Boundary

Future unchanged retries of local-factor positivity, support-only positivity,
normalized `L2`, or broad AP source matching should be treated as repeats of a
blocked conjunction.  q286 may still be used as coefficient structure,
finite-calibration/falsifier evidence, visualization substrate, or as the
target for a genuinely raw pointwise theorem.

This establishes no signed-weight major/minor arc estimate, no raw weighted
witness theorem, no active-character moment theorem, no positive-mass theorem,
no q286 threshold theorem, no strict-central Goldbach theorem, and no Goldbach
proof.
