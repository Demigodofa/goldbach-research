# q286 non-circular route triage audit

## Question

After the zero-mass check passed but the WBSS aggregate `L2` cap failed on
finite rows, which q286 route is the sharper non-circular theorem obligation?

## Receipt

```text
tools/build_q286_non_circular_route_triage_audit.py
evidence/q286-non-circular-route-triage-audit.json
```

## Result

The plain WBSS aggregate `L2` bridge is moved to reservoir until a changed
condition appears.  It is still coefficient algebra and a possible asymptotic
theorem shape, but it is not an already-valid bridge:

```text
checked WBSS rows:                 348
row-local L2 cap violations:       120
global-minimum L2 cap violations:  301
worst target:                  1089544
worst row-local ratio:              1.5570989543984672
zero-mass defect found:             no
non-circular bridge confirmed:      no
```

The component-pair route is the sharper current theorem obligation because it
localizes the missing theorem to an active selector, a combined-driver floor,
and a fixed-conductor real-channel bound:

```text
selected late active rows:                    3
positive strict closure margins:              3 / 3
calibrated driver floor:        -0.10172538432746947
normalized real-channel Linf cap: 0.05885324711081062
tightest selected target:                 1379072
tightest selected margin:       0.48379401372791037
```

## Decision

The next best non-circular theorem target is the component-pair active-lane
driver floor plus fixed-conductor real-channel bound.  This does not prove
Goldbach and does not establish the component-pair theorem.  It only ranks the
current routes: plain WBSS `L2` finite cap checks should sleep unless a new
external pointwise moment theorem, a stronger structured signed moment theorem,
or a different unnormalized signed estimate appears.

The smallest useful next test is either to prove or falsify the
active-selector rarity statement, or to apply the unchanged component-pair
closure scalar to the next predeclared active rows while keeping scanned,
selected, stressed, passed, failed, and not-applicable counts separate.
