# Goldbach graph curiosity read-only signal

## Question

Kevin asked whether anything in the existing graphs jumps out, and then
clarified the boundary: do not change or build a graph merely because the idea
was suggested.  Does the current route graph contain any evidence-bearing shape
signal that should guide a future Rill awakening?

## Read-only Sources

```text
evidence/goldbach-semantic-route-graph.json
evidence/goldbach-bridge-acceptance-gate-audit.json
evidence/q286-wbss-zero-mass-l2-logical-bridge-audit.json
evidence/mobius-moment-square-degree5-source-admissible-window-audit.json
evidence/mobius-moment-square-degree5-checked-scale-phase-curve-sweep.json
```

No new graph or visualization was built for this note.

## Observed Signal

The useful graph-shaped signal is not a picture; it is the separation between
loop-back/churn lanes and one thin but live frontier.

The normalized aggregate `L2` lane is a loop-back under unchanged conditions:
zero-mass arithmetic sanity is confirmed only on the checked rows, the raw
strict `L2` shape is non-circular as a theorem shape, but the bridge is not
confirmed.  The observed normalized lane has `120` row-local cap violations
and `301` global-minimum cap violations.  More finite normalized `L2` scans
therefore look like churn unless the changed condition is a strict raw
pointwise theorem attempt.

The source-window lane is different.  The broad all-translation statement is
finitely falsified by the single translated row

```text
M=149, p=163, active row start 1, component (00,12),
active/full ratio 0.4980466670754268,
slack -0.0019533329245732256.
```

But every canonical source start passes across the six checked scales.  At
the failing scale `M=149`, the canonical source start is `32`, inside the
source-connected passing interval `2..64`.  The weakest canonical source row
is

```text
M=167, p=181, component (00,12),
active/full ratio 0.5563677490893767,
slack 0.056367749089376695.
```

This makes source-window/admissible-window geometry a legitimate thin
frontier, not a Goldbach bridge by itself.

## Decision

Future Rill awakenings should use graph-shaped views only if they change the
next theorem or falsifier action.  A heat map, 3D scatterplot, route graph, or
table is justified only when it exposes at least one of:

```text
a theorem obligation,
a falsifier target,
a low-return loop,
a thin frontier,
or a changed condition for reviving a held route.
```

Current action guidance:

```text
source-window/admissible-window geometry: worth theorem or falsifier work
normalized aggregate L2 finite scans: churn unless changed to strict raw theorem
finite adverse-drag passes: calibration/falsifier evidence only, not acceptance
live bridge target: universal pointwise raw adverse-drag
```

This note is a navigation aid only.  It proves no visualization theorem,
source-window theorem, aggregate `L2` theorem, raw adverse-drag theorem,
q286 threshold theorem, strict-central Goldbach theorem, or Goldbach proof.
